# ============================================
# Python 后端 - 只提交源代码
# ============================================

# Python 虚拟环境（可重新创建）
venv/
.venv/
env/
ENV/
.Python

# Python 编译文件（自动生成）
__pycache__/
*.py[cod]
*$py.class
*.so
*.pyc

# 环境变量（敏感信息，不提交）
.env

# 日志文件（运行时生成）
logs/*.log

# ============================================
# Storage 目录 - 用户数据，不提交
# ============================================

# Storage 根目录（忽略所有文件，但保留目录结构）
storage/*
!storage/.gitkeep

# Uploads 目录（用户上传文件）
storage/uploads/*
!storage/uploads/.gitkeep
!storage/uploads/temp/.gitkeep
!storage/uploads/avatars/.gitkeep
!storage/uploads/covers/.gitkeep
!storage/uploads/subtitles/.gitkeep

# Videos 目录（视频文件）
storage/videos/*
!storage/videos/.gitkeep
!storage/videos/originals/.gitkeep
!storage/videos/hls/.gitkeep

# 数据库文件（本地数据，不提交）
*.db
*.sqlite
*.sqlite3

# 测试覆盖率报告（自动生成）
.pytest_cache/
.coverage
htmlcov/
.tox/

# 分发和打包（自动生成）
dist/
build/
*.egg-info/
